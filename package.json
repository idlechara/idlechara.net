{
  "name": "idlechara-moe",
  "private": true,
  "type": "module",
  "scripts": {
    "build-posts": "node scripts/build-posts.js",
    "dev": "node scripts/build-posts.js && vite",
    "dev:offline": "SKIP_LASTFM=1 node scripts/build-posts.js && vite",
    "build": "node scripts/build-posts.js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0",
    "vite": "^5.4.0"
  }
}
